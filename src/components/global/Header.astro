---
import { Image } from "astro:assets";
import logoImg from "../../assets/images/icons/powernet-logo.png";

const pathname = new URL(Astro.request.url).pathname;
const isHome = pathname === "/" || pathname === "/index.html";
const navItems = [
  { href: "/#services", label: "Services" },
  { href: "/#projects", label: "Projects" },
  { href: "/#about", label: "About" },
  { href: "/#contact", label: "Contact" },
];
const talkLabel = "Let's Talk";
---

<header
  id="main-header"
  class={`header ${isHome ? "header-transparent-dark" : ""}`}
>
  <div class="container header-container">
    <div class="logo">
      <a href="/" class="logo-link">
        <Image
          src={logoImg}
          alt="Powernet Logo"
          class="logo-img"
          loading="eager"
          fetchpriority="high"
        />
      </a>
    </div>

    <div class="original-nav-container">
      <nav class="desktop-nav">
        <ul class="nav-list">
          {
            navItems.map((item) => (
              <li>
                <a href={item.href} class="nav-link" aria-label={item.label}>
                  <span class="nav-word" aria-hidden="true">
                    <span class="nav-layer nav-layer-top">
                      {Array.from(item.label).map((letter, index) => (
                        <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                          {letter}
                        </span>
                      ))}
                    </span>
                    <span class="nav-layer nav-layer-bottom" aria-hidden="true">
                      {Array.from(item.label).map((letter, index) => (
                        <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                          {letter}
                        </span>
                      ))}
                    </span>
                  </span>
                </a>
              </li>
            ))
          }
        </ul>
      </nav>

      <button
        id="mobile-menu-toggle"
        class="mobile-menu-btn hamburger"
        aria-label="Toggle menu"
        aria-expanded="false"
      >
        <span class="hamburger-line"></span>
        <span class="hamburger-line"></span>
      </button>

      <div class="header-actions">
        <a href="#contact" class="nav-btn-talk" aria-label={talkLabel}>
          <span class="nav-word" aria-hidden="true">
            <span class="nav-layer nav-layer-top">
              {Array.from(talkLabel).map((letter, index) => (
                <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                  {letter === " " ? "\u00A0" : letter}
                </span>
              ))}
            </span>
            <span class="nav-layer nav-layer-bottom" aria-hidden="true">
              {Array.from(talkLabel).map((letter, index) => (
                <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                  {letter === " " ? "\u00A0" : letter}
                </span>
              ))}
            </span>
          </span>
        </a>
      </div>
    </div>
  </div>

  <!-- mobile drawer container -->
  <div id="mobile-nav-overlay" class="mobile-nav nav-links-wrapper" aria-hidden="true">
    <div class="mobile-nav-content">
      <ul class="mobile-nav-list">
        {
          navItems.map((item) => (
            <li>
              <a href={item.href} class="mobile-nav-link">
                {item.label}
              </a>
            </li>
          ))
        }
      </ul>
      <div class="mobile-nav-actions">
        <a href="#contact" class="nav-btn-talk mobile-cta" aria-label={talkLabel}>
          <span class="nav-word" aria-hidden="true">
            <span class="nav-layer nav-layer-top">
              {Array.from(talkLabel).map((letter, index) => (
                <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                  {letter === " " ? "\u00A0" : letter}
                </span>
              ))}
            </span>
            <span class="nav-layer nav-layer-bottom" aria-hidden="true">
              {Array.from(talkLabel).map((letter, index) => (
                <span class="nav-char" style={`--i:${index}; --char-index:${index}`}>
                  {letter === " " ? "\u00A0" : letter}
                </span>
              ))}
            </span>
          </span>
        </a>
      </div>
    </div>
  </div>
</header>

<style>
  /* root header stacking */
  header, 
  .site-header,
  .header,
  #main-header {
    position: fixed !important;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 9999 !important;
    padding-top: 0;
    background-color: transparent;
    border-bottom: 1px solid transparent;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    transform: translateY(0);
    transition: transform 0.4s cubic-bezier(0.16, 1, 0.3, 1),
      background-color 0.3s ease,
      border-color 0.3s ease,
      backdrop-filter 0.3s ease,
      -webkit-backdrop-filter 0.3s ease;
    pointer-events: none;
  }

  /* directional scroll states */
  #main-header.is-hidden {
    transform: translateY(-100%);
  }

  #main-header.is-scrolled {
    transform: translateY(0);
    background-color: rgba(13, 14, 18, 0.94);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  #main-header.is-top {
    transform: translateY(0);
    background-color: transparent;
    border-bottom: 1px solid transparent;
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }

  .header-container {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    width: 95%;
    max-width: 1400px;
    margin: 0 auto;
    min-height: 80px;
    pointer-events: none;
    position: relative;
    transition: min-height 0.3s ease;
  }

  .original-nav-container {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 24px;
    margin-top: 20px;
    margin-bottom: 20px;
    pointer-events: auto;
    transition: margin 0.3s ease;
  }

  #main-header.is-scrolled .original-nav-container {
    margin-top: 12px;
    margin-bottom: 12px;
  }

  /* logo anchor */
  .logo {
    position: absolute;
    top: 0;
    left: 0;
    z-index: 50;
    pointer-events: auto;
  }

  .logo-link {
    display: flex;
    align-items: center;
    justify-content: center;
    box-sizing: border-box;
    padding: 16px 36px 24px 24px;
    background: rgba(255, 255, 255, 0.98);
    border-radius: 0 0 42px 0;
    text-decoration: none;
    box-shadow: 4px 8px 24px rgba(0, 0, 0, 0.12);
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
    transition: transform 0.3s ease, padding 0.3s ease, border-radius 0.3s ease;
  }

  #main-header.is-scrolled .logo-link {
    padding: 12px 28px 18px 20px;
    border-radius: 0 0 28px 0;
  }

  .logo-img {
    display: block;
    width: 150px;
    max-width: 100%;
    height: auto;
    object-fit: contain;
  }

  /* desktop navigation */
  .desktop-nav {
    background: rgba(20, 20, 20, 0.85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-radius: 50px;
    padding: 12px 32px;
    border: 1px solid rgba(255, 255, 255, 0.05);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    margin: 0;
  }

  .nav-list {
    display: flex;
    gap: 36px;
    list-style: none;
    margin: 0;
    padding: 0;
    transition: gap 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .nav-link {
    font-family: var(--font-mono);
    font-size: 0.95rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: #ffffff;
    text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
    text-decoration: none;
    display: inline-block;
    height: 1.2em;
    overflow: hidden;
    opacity: 0.9;
    transition:
      color 0.6s cubic-bezier(0.22, 1, 0.36, 1),
      text-shadow 0.6s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .nav-word {
    position: relative;
    display: block;
    height: 1.2em;
    line-height: 1.2em;
    overflow: hidden;
  }

  .nav-layer {
    display: flex;
    white-space: nowrap;
  }

  .nav-layer-bottom {
    position: absolute;
    left: 0;
    top: 100%;
  }

  .nav-char {
    display: inline-block;
    transition: transform 0.45s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: calc(var(--char-index, var(--i)) * 20ms);
  }

  .nav-link:hover .nav-layer-top .nav-char,
  .nav-link:hover .nav-layer-bottom .nav-char,
  .nav-link:focus-visible .nav-layer-top .nav-char,
  .nav-link:focus-visible .nav-layer-bottom .nav-char,
  .nav-btn-talk:hover .nav-layer-top .nav-char,
  .nav-btn-talk:hover .nav-layer-bottom .nav-char,
  .nav-btn-talk:focus-visible .nav-layer-top .nav-char,
  .nav-btn-talk:focus-visible .nav-layer-bottom .nav-char {
    transform: translateY(-100%);
  }

  .nav-link:hover {
    opacity: 1;
    color: var(--color-brand, #e63946);
  }

.nav-btn-talk,
.btn-solid-red {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 48px;
  padding: 0 28px;
  border-radius: 9999px;
  background-color: #d62828;
  color: #ffffff !important;
  font-size: 0.95rem;
  font-weight: 600;
  text-decoration: none;
  white-space: nowrap;
  border: none;
  cursor: pointer;
  box-sizing: border-box;
  overflow: hidden;
  transition: background-color 0.2s ease, transform 0.15s ease;
}

.nav-btn-talk:hover,
.btn-solid-red:hover {
  background-color: #b91d1d;
}

.nav-btn-talk:active,
.btn-solid-red:active {
  transform: scale(0.97);
}

.btn-frosted {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 48px;
  padding: 0 28px;
  border-radius: 9999px;
  background: rgba(255, 255, 255, 0.12);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.25);
  color: #ffffff !important;
  font-size: 0.95rem;
  font-weight: 600;
  text-decoration: none;
  white-space: nowrap;
  cursor: pointer;
  box-sizing: border-box;
  transition: background-color 0.2s ease, border-color 0.2s ease, transform 0.15s ease;
}

.btn-frosted:hover {
  background: rgba(255, 255, 255, 0.18);
  border-color: rgba(255, 255, 255, 0.45);
}

.btn-frosted:active {
  transform: scale(0.97);
}

  /* mobile menu toggle */
  .mobile-menu-btn {
    display: none;
    flex-direction: column;
    justify-content: center;
    gap: 6px;
    background: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 50px;
    cursor: pointer;
    padding: 12px 16px;
    z-index: 10000;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }

  .hamburger-line {
    width: 20px;
    height: 2px;
    background-color: #ffffff;
    transition:
      transform 0.3s ease,
      opacity 0.3s ease;
  }

  .mobile-menu-btn.is-active {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .mobile-menu-btn.is-active .hamburger-line {
    background-color: #ffffff;
  }

  .mobile-menu-btn.is-active .hamburger-line:first-child {
    transform: translateY(4px) rotate(45deg);
  }

  .mobile-menu-btn.is-active .hamburger-line:last-child {
    transform: translateY(-4px) rotate(-45deg);
  }

  /* responsive queries */
  @media (min-width: 1025px) {
    .mobile-nav,
    .nav-links-wrapper {
      display: none;
    }
  }

  @media (max-width: 1024px) {
    #main-header {
      top: 0;
    }

    .header-container {
      min-height: 70px;
    }

    .logo-link {
      padding: 12px 20px 16px 20px;
      border-radius: 0 0 24px 24px;
    }

    .logo-img {
      width: 120px;
    }

    .desktop-nav,
    .header-actions {
      display: none;
    }

    .original-nav-container {
      margin-top: 14px;
      margin-bottom: 14px;
    }

    /* explicit mobile menu toggle */
    .mobile-menu-btn,
    .hamburger, 
    #mobile-menu-toggle {
      display: flex !important;
      opacity: 1 !important;
      visibility: visible !important;
      position: relative;
      z-index: 10000 !important; 
      pointer-events: auto !important;
      cursor: pointer;
    }

    /* mobile drawer */
    .mobile-nav,
    .nav-links-wrapper {
      position: fixed;
      top: 76px;
      left: 16px;
      right: 16px;
      width: auto;
      padding: 28px 24px;
      background: rgba(13, 14, 18, 0.98);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.08);
      border-radius: 20px;
      box-shadow: 0 24px 48px rgba(0, 0, 0, 0.6);
      display: flex;
      flex-direction: column;
      gap: 16px;
      transform: translateY(-16px) scale(0.98);
      opacity: 0;
      pointer-events: none;
      visibility: hidden;
      transition: transform 0.35s cubic-bezier(0.16, 1, 0.3, 1),
        opacity 0.3s ease,
        visibility 0.3s ease;
      z-index: 9998;
    }

    .mobile-nav.is-active,
    .nav-links-wrapper.is-active {
      transform: translateY(0) scale(1);
      opacity: 1;
      pointer-events: auto;
      visibility: visible;
    }

    .mobile-nav-content {
      display: flex;
      flex-direction: column;
      gap: 16px;
      width: 100%;
    }

    .mobile-nav-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      width: 100%;
    }

    .mobile-nav-link {
      font-size: 1rem;
      font-weight: 600;
      letter-spacing: 0.08em;
      color: #ffffff !important;
      text-decoration: none;
      padding: 14px 4px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.06);
      transition: color 0.2s ease;
      display: block;
      text-transform: uppercase;
    }

    .mobile-nav-link:hover {
      color: var(--color-brand, #d62828) !important;
    }

    .mobile-nav-actions {
      width: 100%;
      margin-top: 8px;
    }

    .mobile-cta {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
      box-sizing: border-box;
      padding: 14px 20px;
      border-radius: 9999px;
      background-color: var(--color-brand, #d62828);
      color: #ffffff;
      font-weight: 700;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      font-size: 0.85rem;
      border: none;
    }
  }

  @media (max-width: 768px) {
    .header-container {
      min-height: 64px;
    }

    .original-nav-container {
      margin-top: 10px;
      margin-bottom: 10px;
    }

    .mobile-nav,
    .nav-links-wrapper {
      top: 68px;
      padding: 24px 18px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .nav-char {
      transition: none !important;
      transform: none !important;
    }
  }
</style>

<script>
  /* directional scroll and drawer lifecycle */
  let headerScrollHandler: (() => void) | null = null;
  let headerClickHandler: ((e: MouseEvent) => void) | null = null;

  const initHeader = () => {
    const header = document.querySelector("header") as HTMLElement | null;
    const menuBtn = document.getElementById("mobile-menu-toggle");
    const mobileNav = document.getElementById("mobile-nav-overlay");

    if (!header) return;

    if (headerScrollHandler) {
      window.removeEventListener("scroll", headerScrollHandler);
      headerScrollHandler = null;
    }
    if (headerClickHandler) {
      document.removeEventListener("click", headerClickHandler);
      headerClickHandler = null;
    }

    let lastScrollY = Math.max(window.scrollY, 0);
    let ticking = false;
    let isMenuOpen = false;
    const DELTA_THRESHOLD = 8;

    const onScroll = () => {
      /* drawer open lock */
      if (isMenuOpen) {
        lastScrollY = Math.max(window.scrollY, 0);
        ticking = false;
        return;
      }

      const currentScrollY = Math.max(window.scrollY, 0);
      const delta = currentScrollY - lastScrollY;

      // ignore micro-jitters
      if (Math.abs(delta) < DELTA_THRESHOLD) {
        ticking = false;
        return;
      }

      // surface behavior at top fold
      if (currentScrollY < 20) {
        header.classList.remove("is-hidden", "is-scrolled");
        header.style.transform = "translate3d(0, 0, 0)";
      } else {
        header.classList.add("is-scrolled");
        if (delta > 0 && currentScrollY > 80) {
          // scrolling down -> hide
          header.classList.add("is-hidden");
          header.style.transform = "translate3d(0, -100%, 0)";
        } else if (delta < 0) {
          // scrolling up -> reveal
          header.classList.remove("is-hidden");
          header.style.transform = "translate3d(0, 0, 0)";
        }
      }

      lastScrollY = currentScrollY;
      ticking = false;
    };

    // initialize state
    if (window.scrollY < 20) {
      header.classList.remove("is-hidden", "is-scrolled");
      header.style.transform = "translate3d(0, 0, 0)";
    } else {
      header.classList.add("is-scrolled");
    }

    headerScrollHandler = () => {
      if (!ticking) {
        window.requestAnimationFrame(onScroll);
        ticking = true;
      }
    };

    window.addEventListener("scroll", headerScrollHandler, { passive: true });

    /* mobile drawer toggle */
    if (menuBtn && mobileNav) {
      const toggleMenu = (open?: boolean) => {
        const willOpen = typeof open === "boolean" ? open : !isMenuOpen;
        isMenuOpen = willOpen;
        menuBtn.setAttribute("aria-expanded", String(willOpen));
        menuBtn.classList.toggle("is-active", willOpen);
        mobileNav.classList.toggle("is-active", willOpen);
        mobileNav.setAttribute("aria-hidden", String(!willOpen));
        if (willOpen) {
          header.classList.remove("is-hidden");
          header.classList.add("is-scrolled");
          header.style.transform = "translate3d(0, 0, 0)";
        }
      };

      menuBtn.onclick = (e) => {
        e.stopPropagation();
        toggleMenu();
      };

      mobileNav.querySelectorAll("a").forEach((link) => {
        link.onclick = () => {
          toggleMenu(false);
        };
      });

      /* backdrop click close */
      headerClickHandler = (e: MouseEvent) => {
        if (isMenuOpen && !mobileNav.contains(e.target as Node) && !menuBtn.contains(e.target as Node)) {
          toggleMenu(false);
        }
      };
      document.addEventListener("click", headerClickHandler);
    }
  };

  document.addEventListener("DOMContentLoaded", initHeader);
  document.addEventListener("astro:page-load", initHeader);

  if (document.readyState === "interactive" || document.readyState === "complete") {
    initHeader();
  }
</script>
